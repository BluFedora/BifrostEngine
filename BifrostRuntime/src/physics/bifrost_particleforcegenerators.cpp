#include "particleforcegenerators.h"

namespace prism
{
    void IParticleForceGenerator::updateForce(Particle *particle, real duration)
    {
        (void) particle;
        (void) duration;
    }
}